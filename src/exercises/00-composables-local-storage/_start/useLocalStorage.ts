/**
 * Creates a two-way binding to a local storage property
 *
 * @param storageKey - the key to persist the data
 * @param defaultValue - the default value when no data exists in localStorage
 */
export function useLocalStorage(storageKey: string, defaultValue: any) {
  // we probably need to return something else
  return defaultValue
}
